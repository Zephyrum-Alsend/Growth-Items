scn aaDusfergonShowModelName

;Main script
;-----------------------------------------------------------------------------
;aaDusfergonManager
;-----------------------------------------------------------------------------

;Used scripts
;-----------------------------------------------------------------------------
;-----------------------------------------------------------------------------

;Related scripts
;-----------------------------------------------------------------------------
;aaDusfergonModelChange
;aaDusfergonSetModel
;aaDusfergonSetModel2
;-----------------------------------------------------------------------------

;Used globals
;-----------------------------------------------------------------------------
;aaDusfergonModel
;-----------------------------------------------------------------------------

;Related globals
;-----------------------------------------------------------------------------
;-----------------------------------------------------------------------------

;Description
;-----------------------------------------------------------------------------
;According to the ordering in SetModel(2), displays a message stating the 
;weapon's form's name as it appears in ModelChange's menus, and the sub-type 
;menu that name is found under.
;-----------------------------------------------------------------------------

;Parameters
;-----------------------------------------------------------------------------
string_var ModelName
string_var Icon
string_var Name
;-----------------------------------------------------------------------------



;-----------------------------------------------------------------------------
begin Function { Icon, Name }
;-----------------------------------------------------------------------------

	SetMessageIcon $Icon

	Set ModelName to sv_Construct "%z's current form is " Name

	
	;-----------------------------------------------------------------------------
	;Claymore
	;-----------------------------------------------------------------------------
	if ( aaDusfergonModel == 0 )
	;-----------------------------------------------------------------------------
	
		Set ModelName to sv_Construct "%zBastard" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 1 )
	;-----------------------------------------------------------------------------
	
		Set ModelName to sv_Construct "%zMerciless" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 2 )
	;-----------------------------------------------------------------------------
	
		Set ModelName to sv_Construct "%zWolf" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 3 )
	;-----------------------------------------------------------------------------
	
		Set ModelName to sv_Construct "%zWolf Ricasso" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 4 )
	;-----------------------------------------------------------------------------
	
		Set ModelName to sv_Construct "%zAkatosh" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 5 )
	;-----------------------------------------------------------------------------
	
		Set ModelName to sv_Construct "%zSerenity" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 6 )
	;-----------------------------------------------------------------------------
	
		Set ModelName to sv_Construct "%zZweihander" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 7 )
	;-----------------------------------------------------------------------------
	
		Set ModelName to sv_Construct "%zSunfire" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 8 )
	;-----------------------------------------------------------------------------
	
		Set ModelName to sv_Construct "%zIvellon Claymore" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 9 )
	;-----------------------------------------------------------------------------
	
		Set ModelName to sv_Construct "%zIvellon Zweihander" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 10 )
	;-----------------------------------------------------------------------------
	
		Set ModelName to sv_Construct "%zSathix" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 11 )
	;-----------------------------------------------------------------------------
	
		Set ModelName to sv_Construct "%zSathix Crimson" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 12 )
	;-----------------------------------------------------------------------------
	
		Set ModelName to sv_Construct "%zDecrepit" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 13 )
	;-----------------------------------------------------------------------------
	
		Set ModelName to sv_Construct "%zGotland" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 14 )
	;-----------------------------------------------------------------------------
	
		Set ModelName to sv_Construct "%zGotland Steel" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 15 )
	;-----------------------------------------------------------------------------
	
		Set ModelName to sv_Construct "%zGotland Old" ModelName



		
	;-----------------------------------------------------------------------------
	;Bastard
	;-----------------------------------------------------------------------------
	elseif ( ( aaDusfergonModel == 1000 ) || ( aaDusfergonModel == 2000 ) )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zMercy" ModelName

	;-----------------------------------------------------------------------------
	elseif ( ( aaDusfergonModel == 1001 ) || ( aaDusfergonModel == 2001 ) )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zTrust" ModelName

	;-----------------------------------------------------------------------------
	elseif ( ( aaDusfergonModel == 1002 ) || ( aaDusfergonModel == 2002 ) )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zKinslayer" ModelName

	;-----------------------------------------------------------------------------
	elseif ( ( aaDusfergonModel == 1003 ) || ( aaDusfergonModel == 2003 ) )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zHope" ModelName

	;-----------------------------------------------------------------------------
	elseif ( ( aaDusfergonModel == 1004 ) || ( aaDusfergonModel == 2004 ) )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zDespair" ModelName

	;-----------------------------------------------------------------------------
	elseif ( ( aaDusfergonModel == 1005 ) || ( aaDusfergonModel == 2005 ) )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zSorrow" ModelName

	;-----------------------------------------------------------------------------
	elseif ( ( aaDusfergonModel == 1006 ) || ( aaDusfergonModel == 2006 ) )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zTruth" ModelName

	;-----------------------------------------------------------------------------
	elseif ( ( aaDusfergonModel == 1007 ) || ( aaDusfergonModel == 2007 ) )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zType-A" ModelName

	;-----------------------------------------------------------------------------
	elseif ( ( aaDusfergonModel == 1008 ) || ( aaDusfergonModel == 2008 ) )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zType-B" ModelName

	;-----------------------------------------------------------------------------
	elseif ( ( aaDusfergonModel == 1009 ) || ( aaDusfergonModel == 2009 ) )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zType-C" ModelName

	;-----------------------------------------------------------------------------
	elseif ( ( aaDusfergonModel == 1010 ) || ( aaDusfergonModel == 2010 ) )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zType-D" ModelName

	;-----------------------------------------------------------------------------
	elseif ( ( aaDusfergonModel == 1011 ) || ( aaDusfergonModel == 2011 ) )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zType-E" ModelName

	;-----------------------------------------------------------------------------
	elseif ( ( aaDusfergonModel == 1012 ) || ( aaDusfergonModel == 2012 ) )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zHiero" ModelName

	;-----------------------------------------------------------------------------
	elseif ( ( aaDusfergonModel == 1013 ) || ( aaDusfergonModel == 2013 ) )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zSurazal" ModelName

	;-----------------------------------------------------------------------------
	elseif ( ( aaDusfergonModel == 1014 ) || ( aaDusfergonModel == 2014 ) )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zIvellon" ModelName

	;-----------------------------------------------------------------------------
	elseif ( ( aaDusfergonModel == 1015 ) || ( aaDusfergonModel == 2015 ) )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zWraith" ModelName

	;-----------------------------------------------------------------------------
	elseif ( ( aaDusfergonModel == 1016 ) || ( aaDusfergonModel == 2016 ) )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zClint XVI" ModelName

	;-----------------------------------------------------------------------------
	elseif ( ( aaDusfergonModel == 1017 ) || ( aaDusfergonModel == 2017 ) )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zAngular Leaf" ModelName

	;-----------------------------------------------------------------------------
	elseif ( ( aaDusfergonModel == 1018 ) || ( aaDusfergonModel == 2018 ) )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zTapper" ModelName

	;-----------------------------------------------------------------------------
	elseif ( ( aaDusfergonModel == 1019 ) || ( aaDusfergonModel == 2019 ) )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zSeaward" ModelName

	;-----------------------------------------------------------------------------
	elseif ( ( aaDusfergonModel == 1020 ) || ( aaDusfergonModel == 2020 ) )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zClint XVI Steel" ModelName

	;-----------------------------------------------------------------------------
	elseif ( ( aaDusfergonModel == 1021 ) || ( aaDusfergonModel == 2021 ) )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zAngular Leaf Steel" ModelName

	;-----------------------------------------------------------------------------
	elseif ( ( aaDusfergonModel == 1022 ) || ( aaDusfergonModel == 2022 ) )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zTapper Steel" ModelName

	;-----------------------------------------------------------------------------
	elseif ( ( aaDusfergonModel == 1023 ) || ( aaDusfergonModel == 2023 ) )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zTapper Black" ModelName

	;-----------------------------------------------------------------------------
	elseif ( ( aaDusfergonModel == 1024 ) || ( aaDusfergonModel == 2024 ) )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zTapper Old" ModelName



		
	;-----------------------------------------------------------------------------
	;Longsword
	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 3000 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zIron" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 3001 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zFine Iron" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 3002 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zSteel" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 3003 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zFine Steel" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 3004 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zSilver" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 3005 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zEbony" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 3006 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zDaedric" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 3007 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zArming" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 3008 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zIvellon" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 3007 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zCorralor" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 3010 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zGull" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 3011 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zMoonbrand" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 3012 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zArcher" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 3013 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zSolingen" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 3014 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zTritonia" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 3015 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zNecro" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 3016 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zSlavatio" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 3017 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zGoblin" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 3018 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zLeaf" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 3019 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zShokuto" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 3020 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zBaskethilt" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 3021 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zSingle Edge" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 3022 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zDragoon" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 3023 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zKriss" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 3024 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zElvin" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 3025 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zBroke Back" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 3026 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zBone Daito" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 3027 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zDragonfly" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 3028 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zChimer" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 3029 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zCommander" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 3030 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zHook" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 3031 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zScimitar" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 3032 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zVolsung" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 3033 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zAtmorian" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 3034 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zBeserker" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 3035 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zCawood" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 3036 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zCawood II" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 3037 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zGramr" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 3038 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zNaegling" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 3039 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zNorland" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 3040 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zWarp" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 3041 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zArcher Steel" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 3042 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zSolingen Steel" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 3043 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zTritonia Steel" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 3044 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zNecro Steel" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 3045 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zSalvatio Steel" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 3046 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zBaskethilt Steel" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 3047 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zBeserker Steel" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 3048 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zWarp Steel" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 3049 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zLeaf Old" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 3050 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zShokuto Old" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 3051 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zChimer Old" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 3052 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zCommander Old" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 3053 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zHook Old" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 3054 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zScimitar Old" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 3055 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zAtmorian Old" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 3056 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zBeserker Old" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 3057 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zCawood Old" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 3058 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zCawood II Old" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 3059 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zGramr Old" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 3060 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zNaegling Old" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 3061 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zNorland Old" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 3062 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zWarp Old" ModelName



		
	;-----------------------------------------------------------------------------
	;Shortsword
	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 4000 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zIron" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 4001 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zFine Iron" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 4002 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zSteel" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 4003 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zFine Steel" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 4004 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zSilver" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 4005 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zEbony" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 4006 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zIvellon" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 4007 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zBarrow" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 4008 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zArcher" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 4009 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zArcher Kriss" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 4010 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zKriss" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 4011 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zBroad Kriss" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 4012 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zBarbed Leaf" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 4013 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zPanther Leaf" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 4014 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zCorralnium" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 4015 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zLemurian" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 4016 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zBroad Sword" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 4017 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zBroad Blade" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 4018 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zElhazan" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 4019 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zSeaward" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 4020 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zKnuckle Bow" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 4021 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zLion Claw" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 4022 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zElvin" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 4023 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zCommander" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 4024 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zKukri" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 4025 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zFalshion" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 4026 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zFalshion Ranger" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 4027 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zQuandao" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 4028 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zEagle" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 4029 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zCrescent" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 4030 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zOrcish" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 4031 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zMoriche" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 4032 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zCurved" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 4033 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zGoblin Curved" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 4034 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zCleaver" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 4035 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zBone Shokuto" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 4036 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zBone Talon" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 4037 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zBone Wakizaki" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 4038 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zBone Dah" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 4039 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zMeyer" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 4040 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zMoria" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 4041 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zPoignard" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 4042 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zFalshion Steel" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 4043 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zFalshion Ranger Steel" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 4044 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zPoignard Steel" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 4045 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zCommander Old" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 4046 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zEagle Old" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 4047 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zCrescent Old" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 4048 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zMoriche Old" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 4049 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zMeyer Old" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 4050 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zPoignard Old" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 4051 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zBroken" ModelName



		
	;-----------------------------------------------------------------------------
	;Dagger
	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 5000 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zIron" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 5001 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zFine Iron" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 5002 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zSteel" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 5003 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zFine Steel" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 5004 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zSilver" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 5005 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zEbony" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 5006 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zCorralnium" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 5007 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zKriss" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 5008 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zTetela" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 5009 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zSpike" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 5010 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zStiletto" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 5011 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zBowie" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 5012 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zHunting" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 5013 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zMiscitar" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 5014 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zJambaya" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 5015 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zSilver Knife" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 5016 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zMori" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 5017 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zElvin" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 5018 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zJody" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 5019 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zClaw" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 5020 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zCrescent" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 5021 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zBone Tanto" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 5022 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zIvory" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 5023 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zPoignard" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 5024 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zPoignard II" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 5025 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zBroadedge" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 5026 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zSpike Steel" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 5027 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zStiletto Silver" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 5028 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zPoignard Steel" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 5029 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zPoignard II Steel" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 5030 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zBroadedge Steel" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 5031 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zJody Old" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 5032 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zClaw Old" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 5033 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zCrescent Old" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 5034 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zPoignard Old" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 5035 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zPoignard II Old" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 5036 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zBroadedge Old" ModelName



		
	;-----------------------------------------------------------------------------
	;Warhammer
	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 6000 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zFlanged" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 6001 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zMorningstar" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 6002 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zMormacil" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 6003 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zMaul" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 6004 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zStendarr" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 6005 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zGoblinstar" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 6006 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zMirondil" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 6007 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zMirondil Gilded" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 6008 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zFlanged Steel" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 6009 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zMorningstar Steel" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 6010 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zKanabo" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 6011 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zKanabo II" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 6012 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zKanabo Fine" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 6013 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zKanabo Copper" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 6014 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zKanabo Steel" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 6015 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zKanabo Fine Steel" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 6016 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zKanabo Marble" ModelName

	

		
	;-----------------------------------------------------------------------------
	;Battleaxe
	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 7000 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zBearded" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 7001 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zBearded Fine" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 7002 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zWar" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 7003 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zCleaver" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 7004 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zIvellon" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 7005 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zRhino" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 7006 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zSotonhorian" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 7007 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zThorgeir" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 7008 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zZiragon" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 7009 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zBearded Old" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 7010 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zWar Old" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 7011 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zCurved" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 7012 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zIron" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 7013 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zSteel" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 7014 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zDouble" ModelName

	

		
	;-----------------------------------------------------------------------------
	;Mace
	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 8000 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zWarhammer" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 8001 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zIvellon Warhammer" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 8002 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zIvellon Mace" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 8003 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zThame" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 8004 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zMorningstar" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 8005 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zMacil" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 8006 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zWarpick" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 8007 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zSotonhorian" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 8008 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zZiragon Warhammer" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 8009 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zZiragon Warpick" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 8010 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zMorningstar Steel" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 8011 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zHammer" ModelName

	

		
	;-----------------------------------------------------------------------------
	;Waraxe
	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 9000 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zBearded" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 9001 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zBearded Fine" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 9002 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zWar" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 9003 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zSotonhorian" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 9004 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zThorgeir" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 9005 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zZiragon" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 9006 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zMirondil" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 9007 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zMirondil Gilded" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 9008 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zBearded Old" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 9009 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zWar Old" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 9010 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zBent" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 9011 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zCurved" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 9012 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zIron" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 9013 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zSteel" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 9014 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zMithril" ModelName

	


	;-----------------------------------------------------------------------------
	;Bow
	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 10000 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zRanger" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 10001 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zRanger Ivory" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 10002 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zIvellon" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 10003 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zStendarr" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 10004 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zBreton" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 10005 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zBreton Black" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 10006 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zRecurve" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 10007 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zRecurve Leather" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 10008 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zRecurve Steel" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 10009 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zNil Rosewood" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 10010 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zNil Ebony" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 10011 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zGoblin" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 10012 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zGoblin Black" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 10013 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zAshlander" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 10014 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zAshlander II" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 10015 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zIvory" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 10016 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zIvory Wood" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 10017 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zRedwood" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 10018 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zBone" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 10019 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zClassic" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 10020 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zHorn" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 10021 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zHorn II" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 10022 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zSacred Horn" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 10023 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zSpringsteel" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 10024 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zSpringsteel Bell" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 10025 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zSpringsteel Leather" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 10026 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zNord" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 10027 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zFine Nord" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 10028 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zNord Magic" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 10029 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zNord Magic II" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 10030 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zNord Old" ModelName




	;-----------------------------------------------------------------------------
	;Staff
	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 11000 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zIron" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 11001 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zSteel" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 11002 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zSilver" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 11003 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zDwarven" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 11004 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zElven" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 11005 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zGlass" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 11006 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zEbony" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 11007 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zDaedric" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 11008 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zRider" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 11009 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zDruid" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 11010 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zNecro" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 11011 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zRed Varla" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 11012 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zCamoran" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 11013 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zArchmage" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 11014 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zMagnus" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 11015 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zMagnus Archaic" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 11016 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zChaos" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 11017 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zChaos Red" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 11018 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zAyleid" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 11019 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zAyleid Red" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 11020 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zAyleid Wood" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 11021 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zAyleid Wood Thin" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 11022 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zCrystal Green" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 11023 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zCrystal Blue" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 11024 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zCrystal Yellow" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 11025 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zCrystal Orange" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 11026 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zFine Crystal" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 11027 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zFine Cyrstal White" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 11028 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zFine Crystal Green" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 11029 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zFine Crystal Orange" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 11030 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zBlack Iron" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 11031 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zVine Blue" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 11032 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zVine White" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 11033 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zVine Green" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 11034 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zVine Orange" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 11035 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zWood-A" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 11036 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zWood-B" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 11037 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zWood-C" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 11038 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zWood-D" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 11039 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zWood-E" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 11040 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zWood-F" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 11041 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zWood-G" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 11042 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zShepherd" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 11043 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zGrasp" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 11044 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zSkull" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 11045 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zSkull Horned" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 11046 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zWood Gem" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 11047 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zWood Gem White" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 11048 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zWood Gem Green" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 11049 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zWood Gem Orange" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 11050 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zTear-A" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 11051 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zTear-B" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 11052 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zTear-C" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 11053 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zTear-D" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 11054 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zTear-E" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 11055 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zBirch" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 11056 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zHemlock" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 11057 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zMaple" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 11058 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zOak" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 11059 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zOak Thin" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 11060 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zPine" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 11061 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zScotch Pine" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 11062 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zBirch Moss" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 11063 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zMaple Moss" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 11064 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zOak Moss" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 11065 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zOak Thin Moss" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 11066 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zPine Moss" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 11067 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zVanilla Mage-A" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 11068 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zVanilla Mage-B" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 11069 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zVanilla Mage-C" ModelName




	;-----------------------------------------------------------------------------
	;Vanilla
	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 16 || aaDusfergonModel == 3061 || aaDusfergonModel == 4052 || aaDusfergonModel == 5037 || aaDusfergonModel == 6017 || aaDusfergonModel == 7015 || aaDusfergonModel == 8012 || aaDusfergonModel == 9015 || aaDusfergonModel == 10035 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zVanilla Iron" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 17 || aaDusfergonModel == 3062 || aaDusfergonModel == 4053 || aaDusfergonModel == 5038 || aaDusfergonModel == 6018 || aaDusfergonModel == 7016 || aaDusfergonModel == 8013 || aaDusfergonModel == 9016 || aaDusfergonModel == 10036 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zVanilla Steel" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 18 || aaDusfergonModel == 3063 || aaDusfergonModel == 4054 || aaDusfergonModel == 5039 || aaDusfergonModel == 6019 || aaDusfergonModel == 7017 || aaDusfergonModel == 8014 || aaDusfergonModel == 9017 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zVanilla Fine Steel" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 19 || aaDusfergonModel == 3064 || aaDusfergonModel == 4055 || aaDusfergonModel == 5040 || aaDusfergonModel == 6020 || aaDusfergonModel == 7018 || aaDusfergonModel == 8015 || aaDusfergonModel == 9018 || aaDusfergonModel == 10037 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zVanilla Silver" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 20 || aaDusfergonModel == 3065 || aaDusfergonModel == 4056 || aaDusfergonModel == 5041 || aaDusfergonModel == 6021 || aaDusfergonModel == 7019 || aaDusfergonModel == 8016 || aaDusfergonModel == 9019 || aaDusfergonModel == 10038 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zVanilla Dwarven" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 21 || aaDusfergonModel == 3066 || aaDusfergonModel == 4057 || aaDusfergonModel == 5042 || aaDusfergonModel == 6022 || aaDusfergonModel == 7020 || aaDusfergonModel == 8017 || aaDusfergonModel == 9020 || aaDusfergonModel == 10039 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zVanilla Elven" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 22 || aaDusfergonModel == 3067 || aaDusfergonModel == 4058 || aaDusfergonModel == 5043 || aaDusfergonModel == 6023 || aaDusfergonModel == 7021 || aaDusfergonModel == 8018 || aaDusfergonModel == 9021 || aaDusfergonModel == 10040 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zVanilla Glass" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 23 || aaDusfergonModel == 3068 || aaDusfergonModel == 4059 || aaDusfergonModel == 5044 || aaDusfergonModel == 6024 || aaDusfergonModel == 7022 || aaDusfergonModel == 8019 || aaDusfergonModel == 9022 || aaDusfergonModel == 10041 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zVanilla Ebony" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel == 24 || aaDusfergonModel == 3069 || aaDusfergonModel == 4060 || aaDusfergonModel == 5045 || aaDusfergonModel == 6025 || aaDusfergonModel == 7023 || aaDusfergonModel == 8020 || aaDusfergonModel == 9023 || aaDusfergonModel == 10042 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%zVanilla Daedric" ModelName

	;-----------------------------------------------------------------------------
	endif ;( aaDusfergonModel > -1 )
	;-----------------------------------------------------------------------------




	
	;-----------------------------------------------------------------------------
	;Append which sub-type menu it's found in
	;-----------------------------------------------------------------------------
	if ( aaDusfergonModel < aaDusfergonInit.Bastard2hIdx )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%z (Claymore)" ModelName

	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel < aaDusfergonInit.LongswordIdx )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%z (Bastard)" ModelName
		
	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel < aaDusfergonInit.LongswordIdx + 35 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%z (Longsword)" ModelName
		
	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel < aaDusfergonInit.ShortswordIdx )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%z (Longsword II)" ModelName
		
	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel < aaDusfergonInit.ShortswordIdx + 35 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%z (Shortsword)" ModelName
		
	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel < aaDusfergonInit.DaggerIdx )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%z (Shortsword II)" ModelName
		
	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel < aaDusfergonInit.DaggerIdx + 35 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%z (Dagger)" ModelName
		
	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel < aaDusfergonInit.WarhammerIdx )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%z (Dagger II)" ModelName
		
	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel < aaDusfergonInit.BattleaxeIdx )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%z (Warhammer)" ModelName
		
	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel < aaDusfergonInit.MaceIdx )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%z (Battleaxe)" ModelName
		
	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel < aaDusfergonInit.WaraxeIdx )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%z (Mace)" ModelName
		
	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel < aaDusfergonInit.BowIdx )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%z (Waraxe)" ModelName
		
	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel < aaDusfergonInit.BowIdx + 35 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%z (Bow)" ModelName
		
	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel < aaDusfergonInit.StaffIdx )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%z (Bow II)" ModelName
		
	;-----------------------------------------------------------------------------
	elseif ( aaDusfergonModel < aaDusfergonInit.StaffIdx + 35 )
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%z (Staff)" ModelName
		
	;-----------------------------------------------------------------------------
	else
	;-----------------------------------------------------------------------------

		Set ModelName to sv_Construct "%z (Staff II)" ModelName
		
	;-----------------------------------------------------------------------------
	endif ;( aaDusfergonModel > -1 )
	;-----------------------------------------------------------------------------




	
	;-----------------------------------------------------------------------------
	;Print
	;-----------------------------------------------------------------------------
	MessageEX "%z." ModelName 5
	;-----------------------------------------------------------------------------

	sv_Destruct ModelName
;-----------------------------------------------------------------------------
end ;Function { Icon, Name }
;-----------------------------------------------------------------------------